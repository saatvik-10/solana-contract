//seed used for generating the PDA
//should exactly match the one used in the Rust program
export const COUNTER_SEED = 'counter';

//the on-chain program ID of your deployed Solana program
export const PROGRAM_ID = process.env.PROGRAM_ID;
